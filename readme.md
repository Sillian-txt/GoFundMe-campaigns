This repository contains all additional information related to the processes of data collection, parsing, preparation and analysis for my master's thesis: the effects of message framing on campaign succes.

## Dependencies
- Python via the Anaconda distribution
- NLTK dictionaries; open Python, then type
- R and the following packages:

```
install.packages(c("stargazer", "haven", "data.table", "ggplot2"))
```
